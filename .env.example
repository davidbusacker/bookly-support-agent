# Optional: use OpenAI for natural language understanding and responses.
# Without this key the agent runs in mock mode with deterministic logic.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
